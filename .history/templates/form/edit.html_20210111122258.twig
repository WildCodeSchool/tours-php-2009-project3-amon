{% extends 'base.html.twig' %}

{% block title %}Formulaire de modification{% endblock %}

{% block body %}
{{ form_start(form) }}

{{ form_widget(form) }}

<button class="btn">{{ button_label|default('Modifier') }}</button>

{{ form_end(form) }}

<a href="{{path('gallery_metallicDesign') }}">Go to gallery</a>
{% endblock %}